This PR addresses several performance and resilience issues:

### Changes:
- **Lazy-load TransactionConfirmDialog (#747):** Reduced initial bundle size by lazy-loading heavy modals.
- **Route-aware Code Splitting (#750):** Implemented independent chunks per page and optimized `app-router.tsx`.
- **Route-level Error Boundaries (#751):** Added contextual error boundaries with route-specific retry actions.
- **Offline-First PWA Support (#754):** Configured `vite-plugin-pwa` and Service Worker for offline availability.
- **Stability:** Fixed pre-existing unused import errors in `swipeable-quest-card.tsx`.

Verified with `pnpm build` and manual chunk inspection.
